{
  "scripts": {
    "seo:check": "npm --prefix functions run build && node scripts/check-seo-contract.mjs --fixtures",
    "reporting:check": "npm --prefix functions run build && node scripts/check-reporting-contract.mjs",
    "rules:check": "firebase emulators:exec --project demo-missingalert --only firestore,storage \"node --test tests/rules/reporting-rules.test.mjs\"",
    "reporting:emulator:check": "npm --prefix functions run build && firebase emulators:exec --project demo-missingalert --only auth,functions,firestore,storage \"node scripts/check-reporting-emulator.mjs\"",
    "reporting:flow:check": "npm --prefix functions run build && firebase emulators:exec --project demo-missingalert --only auth,functions,firestore,storage \"node scripts/check-reporting-flow-emulator.mjs\"",
    "reporting:browser:check": "playwright test --config playwright.reporting.config.js",
    "reporting:budget:check": "node scripts/check-frontend-budget.mjs"
  },
  "dependencies": {
    "firebase": "^12.3.0",
    "firebase-tools": "^14.18.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@playwright/test": "^1.56.0"
  }
}
