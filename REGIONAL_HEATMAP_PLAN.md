# 지역별 세분화 히트맵 고도화 계획

## 1. 개요
- 목적: 실종 신고 밀집도를 행정 구획(예: 시·군·구 → 읍·면·동)까지 세분화해 사용자에게 고밀도 구역을 직관적으로 제공.
- 범위: 백엔드 집계 파이프라인, 데이터 스키마, 지도 자산, 프런트엔드 시각화, 운영 도구 전반.
- 전제 조건: 최신 행정 경계 데이터 확보, Mapbox 사용 라이선스 및 타일 생성 전략 확정, 데이터 품질 검증 체계 마련.

## 2. 단계별 추진 전략

### 2.1 요구 사항·데이터 기준 확정
- 세분화 목표 구획 단위 정의(행정동, 법정동, GRID 등) 및 우선순위 지역 선정.
- 행정코드, 좌표(GeoJSON), 면적 등 메타데이터 수집 후 표준형식(예: `region_code`, `hierarchy_path`)으로 정리.
- 데이터 신뢰도 진단: 신고 데이터에 행정구 코드/좌표가 포함되는지 확인하고 누락 시 보완 로직 설계.

### 2.2 백엔드 데이터 파이프라인/스키마 확장
- **데이터 적재**: 실종 신고 수집시 세분화 구역 ID를 매핑해 저장(지오코딩/리버스 지오코딩 또는 API 제공 필드를 활용).
- **집계 서비스**: 현재 지역별 집계 모듈(`apiPoller`, `firestore aggregation` 등)을 확장해 새 구획 단위 기준의 통계(`total`, `active`, 기간별 시계열) 생성.
- **캐싱 전략**: 요청 빈도가 높은 구역 데이터를 Redis/Firestore 캐시에 보관하고 TTL/무효화 정책 수립.
- **API 계약**: `/stats/regions` 등 엔드포인트에 `level` 파라미터(예: `province`, `district`, `subdistrict`) 추가, 응답 스키마 문서화.

### 2.3 지도 자산 및 타일 준비
- 최신 행정 구역 GeoJSON 확보(행정안전부, VWorld 등) → 좌표계를 WGS84로 통일.
- Mapbox Tileset 생성: GeoJSON → vector tile 업로드, 속성으로 `region_code`, `display_name`, `parent_code` 포함.
- 타일 스타일 정의: 줌 레벨별로 상위/하위 구역 표시 기준 설정(예: Z<8 시·도, Z>=10 시·군·구, Z>=12 읍·면·동).
- 성능 검증: 타일 크기, 요청 수, 캐시 히트율 확인.

### 2.4 프런트엔드 시각화 개선
- **상호작용 흐름**: 줌/패닝에 따라 히트맵 레이어가 자동으로 세분화 레벨을 전환(줌 인 시 서브 레벨 노출).
- **Mapbox 연동**: 기존 Google Maps 기반 UI와 별도로 Mapbox 레이어 추가 혹은 Mapbox 전환 여부 결정.
- **미니맵/대시보드 갱신**: 미니맵 SVG 경계 대신 타일 또는 세분화 SVG로 교체, 범례·툴팁에 세부 구역 명시.
- **필터링/검색**: 행정구 명으로 검색, 특정 구역 고정/즐겨찾기 기능 추가 검토.
- **반응형 UX**: 모바일에서도 세분화된 히트맵이 부담 없이 표현되도록 레이블 최소화·탭 전환 도입.

### 2.5 성능·안정성·운영
- **캐시 및 배치**: 시계열 대시보드를 위한 사전 집계 배치 작업 스케줄러 점검.
- **모니터링**: Mapbox 타일 사용량, API 지연 시간, 클라이언트 렌더링 비용 수집.
- **QA & 피드백**: 내부 QA 시나리오 작성(줌 단계별 데이터 일치, 툴팁 정확도) → 베타 사용자 피드백 반영.
- **문서화**: 데이터 사전, API 계약서, 운영 매뉴얼 업데이트.

## 3. 예상 리스크 및 대응
- **데이터 누락**: 위치 정보 불충분시 역지오코딩 비용 증가 → 백업 지오코딩 서비스 도입, 실패 시 상위 구획으로 대체 표기.
- **성능 문제**: 타일 레이어/집계 비용 증가 → 캐시 계층 추가, Mapbox expression 최적화, 필요 시 클러스터링 병행.
- **UI 복잡도**: 사용자 혼란 가능 → 단계별 온보딩, 범례/툴팁 강화, 기본 줌 레벨 제한으로 난잡함 방지.

## 4. 결과물 및 일정 제안
- **결과물**: 세분화된 히트맵 UI, 백엔드 API/DB 확장, 지도 타일셋, QA 및 운영 문서.
- **러프 일정(4~6주)**
  1. 주 1~2: 요구사항 확정, 데이터 수집, 아키텍처 설계.
  2. 주 2~4: 백엔드 집계 및 API 개발, 타일셋 구축.
  3. 주 4~5: 프런트엔드 구현, 지도 상호작용/범례 개선.
  4. 주 5~6: 통합 테스트, 성능 튜닝, 문서화 및 내부/베타 검증.

## 5. 후속 과제
- 자동 경계 업데이트 파이프라인 도입(행정구 변경 대응).
- 사용자 정의 알림: 특정 구역 새 사건 발생 시 알림 제공.
- 사고 유형/시간대별 다층 히트맵 시도(예: 야간 집중도 등).
